import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

var mountNode = document.getElementById("app");
ReactDOM.render(<App name="Welcome to first student" />, mountNode);
